<UserControl x:Class="PPC.Shop.Views.ShopDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:PPC.Shop.ViewModels"
             xmlns:saleViewViewModels="clr-namespace:PPC.Sale.ViewModels;assembly=PPC.Sale"
             xmlns:saleViews="clr-namespace:PPC.Sale.Views;assembly=PPC.Sale"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:ShopViewModelDesignData, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:CashRegisterViewModel}">
            <saleViews:ShopView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type saleViewViewModels:ClientViewModel}">
            <saleViews:ClientView DataContext="{Binding}"/>
        </DataTemplate>
    </UserControl.Resources>
    <ContentControl Content="{Binding SelectedButton}"/>
</UserControl>
